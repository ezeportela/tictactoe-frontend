import { WinnerGamePipe } from './winner-game.pipe';

describe('WinnerGamePipe', () => {
  it('create an instance', () => {
    const pipe = new WinnerGamePipe();
    expect(pipe).toBeTruthy();
  });
});
