import { FinishedGamePipe } from './finished-game.pipe';

describe('FinishedGamePipe', () => {
  it('create an instance', () => {
    const pipe = new FinishedGamePipe();
    expect(pipe).toBeTruthy();
  });
});
